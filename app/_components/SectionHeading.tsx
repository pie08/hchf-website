import { styled } from "@linaria/react";
import { FC } from "react";
import { Container } from "./Container";

export const SectionHeading = styled(Container)`
  display: flex;
  align-items: center;
  flex-direction: column;
  gap: 1.2rem;
`;
